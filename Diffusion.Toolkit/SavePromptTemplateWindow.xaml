<local:BorderlessWindow x:Class="Diffusion.Toolkit.SavePromptTemplateWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:local="clr-namespace:Diffusion.Toolkit"
                  mc:Ignorable="d"
                  d:DataContext="{d:DesignInstance local:SavePromptTemplateModel, IsDesignTimeCreatable = True}"
                  WindowStartupLocation="CenterOwner"
                  ShowInTaskbar="False"
                  MenuWidth="0"
                  TitleWidth="*"
                  WindowStyle="ToolWindow"
                  Style="{StaticResource BorderlessWindowStyle}"
                  Title="Save Prompt as Template" Height="550" Width="500">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding Escape}"></KeyBinding>
    </Window.InputBindings>
    <Grid Margin="10,25,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" FontWeight="Bold">Template Name</Label>
        <TextBox Grid.Row="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"/>

        <Label Grid.Row="2" FontWeight="Bold">Category</Label>
        <ComboBox Grid.Row="3" IsEditable="True" Text="{Binding Category, UpdateSourceTrigger=PropertyChanged}"
                  ItemsSource="{Binding Categories}" Height="24" VerticalAlignment="Top" Margin="0,0,0,5"/>

        <Label Grid.Row="4" FontWeight="Bold">Prompt</Label>
        <TextBox Grid.Row="5" Text="{Binding Prompt}" TextWrapping="Wrap" AcceptsReturn="True"
                 Height="80" VerticalScrollBarVisibility="Auto" Margin="0,0,0,5"/>

        <Label Grid.Row="6" FontWeight="Bold">Negative Prompt</Label>
        <TextBox Grid.Row="7" Text="{Binding NegativePrompt}" TextWrapping="Wrap" AcceptsReturn="True"
                 Height="60" VerticalScrollBarVisibility="Auto" Margin="0,0,0,5"/>

        <Grid Grid.Row="8" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Label FontWeight="Bold" FontSize="10">Model</Label>
                <TextBox Text="{Binding Model}" FontSize="10"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                <Label FontWeight="Bold" FontSize="10">Sampler</Label>
                <TextBox Text="{Binding Sampler}" FontSize="10"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="5,0,0,0">
                <Label FontWeight="Bold" FontSize="10">CFG Scale</Label>
                <TextBox Text="{Binding CFGScale}" FontSize="10"/>
            </StackPanel>
            <StackPanel Grid.Column="3" Margin="5,0,0,0">
                <Label FontWeight="Bold" FontSize="10">Steps</Label>
                <TextBox Text="{Binding Steps}" FontSize="10"/>
            </StackPanel>
        </Grid>

        <Label Grid.Row="9" FontWeight="Bold">Notes</Label>

        <Grid Grid.Row="10" Margin="0,3,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="Cancel_OnClick" Margin="0,0,3,0">Cancel</Button>
            <Button Grid.Column="1" Click="Save_OnClick" Margin="3,0,0,0">Save</Button>
        </Grid>
    </Grid>
</local:BorderlessWindow>
