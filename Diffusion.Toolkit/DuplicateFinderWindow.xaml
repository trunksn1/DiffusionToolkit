<local:BorderlessWindow x:Class="Diffusion.Toolkit.DuplicateFinderWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:local="clr-namespace:Diffusion.Toolkit"
                  xmlns:converters="clr-namespace:Diffusion.Toolkit.Converters"
                  mc:Ignorable="d"
                  d:DataContext="{d:DesignInstance local:DuplicateFinderModel, IsDesignTimeCreatable = True}"
                  WindowStartupLocation="CenterOwner"
                  MenuWidth="0"
                  TitleWidth="*"
                  Style="{StaticResource BorderlessWindowStyle}"
                  Title="Duplicate Finder" Height="700" Width="900">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <converters:BoolToVisibilityCollapsedConverter x:Key="boolToVisCol"/>
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding Escape}"></KeyBinding>
    </Window.InputBindings>
    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Controls Bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10,5">
            <Label VerticalAlignment="Center">Similarity Threshold:</Label>
            <Slider x:Name="ThresholdSlider"
                    Value="{Binding Threshold}"
                    Minimum="50" Maximum="100"
                    TickFrequency="5" IsSnapToTickEnabled="True"
                    Width="200" VerticalAlignment="Center" Margin="5,0"/>
            <Label VerticalAlignment="Center" Content="{Binding Threshold, StringFormat={}{0}%}"/>
            <Button x:Name="ScanButton" Click="Scan_OnClick"
                    Padding="10,3" Margin="20,0,0,0" Content="Scan for Duplicates"/>
            <Button Click="MarkForDeletion_OnClick"
                    Padding="10,3" Margin="10,0,0,0" Content="Mark Selected for Deletion"/>
        </StackPanel>

        <!-- Progress Bar -->
        <Grid Grid.Row="1" Margin="10,0,10,5" Visibility="{Binding IsScanning, Converter={StaticResource boolToVisCol}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar Height="20" Value="{Binding Progress}" Maximum="{Binding ProgressMax}"/>
            <Label Grid.Column="1" Content="{Binding Status}" Margin="5,0,0,0"/>
        </Grid>

        <!-- Results — virtualized so only visible groups are rendered -->
        <ListBox Grid.Row="2" ItemsSource="{Binding DuplicateGroups}" Margin="5,0,5,0"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Pixel"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch"
                 SelectionMode="Single"
                 BorderThickness="0"
                 Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="4" Margin="5" Padding="5">
                        <StackPanel>
                            <TextBlock FontWeight="Bold" Margin="0,0,0,5">
                                <Run Text="Group "/>
                                <Run Text="{Binding GroupIndex, Mode=OneWay}"/>
                                <Run Text=" &#x2014; "/>
                                <Run Text="{Binding Count, Mode=OneWay}"/>
                                <Run Text=" images"/>
                            </TextBlock>
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="DarkGray" BorderThickness="1" Margin="3" Padding="3" Width="200">
                                            <StackPanel>
                                                <CheckBox IsChecked="{Binding IsSelected}" HorizontalAlignment="Right" Margin="0,0,0,3"/>
                                                <Image Source="{Binding Path}" Width="180" Height="120" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                <TextBlock Text="{Binding FileName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}" FontSize="10" Margin="0,3,0,0"/>
                                                <TextBlock FontSize="10">
                                                    <Run Text="{Binding Resolution, Mode=OneWay}"/>
                                                    <Run Text=" | "/>
                                                    <Run Text="{Binding FileSizeFormatted, Mode=OneWay}"/>
                                                </TextBlock>
                                                <TextBlock FontSize="10">
                                                    <Run Text="Similarity: "/>
                                                    <Run Text="{Binding Similarity, StringFormat={}{0:F1}%, Mode=OneWay}"/>
                                                </TextBlock>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock>
                    <Run Text="{Binding TotalGroups, Mode=OneWay}"/>
                    <Run Text=" groups, "/>
                    <Run Text="{Binding TotalDuplicates, Mode=OneWay}"/>
                    <Run Text=" duplicate images"/>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</local:BorderlessWindow>
