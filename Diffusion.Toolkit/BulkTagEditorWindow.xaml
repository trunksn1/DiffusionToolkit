<local:BorderlessWindow x:Class="Diffusion.Toolkit.BulkTagEditorWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:local="clr-namespace:Diffusion.Toolkit"
                  xmlns:models="clr-namespace:Diffusion.Toolkit.Models"
                  mc:Ignorable="d"
                  d:DataContext="{d:DesignInstance models:BulkTagEditorModel, IsDesignTimeCreatable = True}"
                  WindowStartupLocation="CenterOwner"
                  ShowInTaskbar="False"
                  MenuWidth="0"
                  TitleWidth="*"
                  WindowStyle="ToolWindow"
                  Style="{StaticResource BorderlessWindowStyle}"
                  Title="Edit Tags" Height="500" Width="350">
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding EscapeCommand}"/>
    </Window.InputBindings>
    <Grid Margin="0,20,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="8,4" Text="{Binding StatusText}" FontWeight="SemiBold"/>

        <ListView Grid.Row="1" Margin="4,4" ItemsSource="{Binding Tags}"
                  BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Name}"
                              IsChecked="{Binding IsChecked}"
                              IsEnabled="{Binding IsEnabled}"
                              IsThreeState="{Binding IsReadOnly}"
                              Margin="2"/>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <Grid Grid.Row="2" Margin="4,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Margin="4,0" VerticalAlignment="Center"
                     Text="{Binding NewTagText, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="NewTagTextBox_KeyDown"/>
            <Button Grid.Column="1" Margin="4,0" Padding="8,2" Content="+"
                    Command="{Binding AddTagCommand}" ToolTip="Add new tag"/>
        </Grid>

        <Grid Grid.Row="3" Margin="4,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="Cancel_OnClick" Content="Cancel" Margin="4,0"/>
            <Button Grid.Column="1" Click="OK_OnClick" Content="OK" Margin="4,0"/>
        </Grid>
    </Grid>
</local:BorderlessWindow>
